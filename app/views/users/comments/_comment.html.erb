<%= form_with model: [post, comment], local: false do |f| %>
  <%= f.text_area :comment, rows: '3', placeholder: "コメントをここに", class:"form-control comment-textarea" %>
  <%= f.submit "コメントする", class: "btn btn-outline-secondary" %>
<% end %>