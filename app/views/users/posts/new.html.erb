<h1>新規投稿</h1>

<%= form_with model: @post, url:'/posts' do |f| %>

  <h4>タイトル</h4>
  <%= f.text_field :title %>

  <h4>本文</h4>
  <%= f.text_area :body %>

  <h4>カテゴリ</h4>
  <%= f.select :category, [["くらし", "living"], ["おでかけ", "trip"], ["ごはん", "food"], ["ざつだん", "talk"]], include_blank: "カテゴリ選択" %>

  <h4>画像</h4>
  <%= f.file_field :post_image, accept: "image/*" %>
  <%= f.submit '投稿' %>

<% end %>